using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class HGLoadProgress : MonoBehaviour {
	public Text LoadProgress;
	// Use this for initialization
	void Start () {
		LoadProgress.text = "0%";
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
